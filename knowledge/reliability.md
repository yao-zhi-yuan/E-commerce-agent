# 服务可靠性知识

order-service 的健康检查依赖数据库连接池、库存 RPC 与消息积压。出现超时时，先查看下游延迟和连接池等待时间，再决定是否重试。

search-service 出现高延迟时，应检查索引刷新、热点查询和下游模型调用。降级策略可以返回较旧索引，但必须在响应中标记数据新鲜度。

支付或写操作必须使用持久化业务键保证幂等。不能使用每次重试都会变化的随机键，也不能把 Redis 缓存当作唯一事实来源。
